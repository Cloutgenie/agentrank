# Server-only (Vercel → Settings → Environment Variables)
# Name must be exactly: ODDS_API_KEY
# Value: paste the key only — no quotes, no ODDS_API_KEY= prefix
ODDS_API_KEY=

# Leave empty on Vercel (uses same-origin /api/* live routes)
NEXT_PUBLIC_API_URL=
